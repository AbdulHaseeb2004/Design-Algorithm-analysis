#include <stdio.h>
#include <string.h>

int main() {
    char source[100], destination[100];

    printf("Enter a string: ");
    scanf("%s", source);

    // Using strcpy to copy the source string to the destination
    strcpy(destination, source);

    printf("Copied string: %s\n", destination);

    return 0;
}
